<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.advu.workflow.dao.database.CxbIntegralVnMapper">

    <sql id="conditions">
        <where>
            <if test="startTime != '' and startTime != null">
                AND dt &gt;= #{startTime}
            </if>
            <if test="endTime != '' and endTime != null">
                AND dt &lt;= #{endTime}
            </if>
            <if test="cnids != null and cnids != ''">
                AND cnid IN
                <foreach collection="cnids" 
                    item="cnid" open="(" separator="," close=")">
                    ${cnid}
                </foreach>
            </if>
        </where>
    </sql>

    <select id="getEarnDataByDay" resultType="Map">
        SELECT
            dt AS timeCycle,
            SUM(earn_integral) AS totalIntegral,
            SUM(earn_user) AS totalUser
        FROM
            cxb_integral_vn
        <include refid="conditions"></include>
        GROUP BY
            timeCycle
    </select>
    
    <select id="getEarnDataByWeek" resultType="Map">
        SELECT
            DATE_FORMAT(dt,'%Y-%u') AS timeCycle,
            SUM(earn_integral) AS totalIntegral,
            SUM(earn_user) AS totalUser
        FROM
            cxb_integral_vn
        <include refid="conditions"></include>
        GROUP BY
            timeCycle
    </select>
    
    <select id="getEarnDataByMonth" resultType="Map">
        SELECT
            DATE_FORMAT(dt,'%Y-%m') AS timeCycle,
            SUM(earn_integral) AS totalIntegral,
            SUM(earn_user) AS totalUser
        FROM
            cxb_integral_vn
        <include refid="conditions"></include>
        GROUP BY
            timeCycle
    </select>
    
    <select id="getEarnSummary" resultType="Map">
        SELECT
            SUM(earn_integral) AS totalIntegral,
            SUM(earn_user) AS totalUser
        FROM
            cxb_integral_vn
        <include refid="conditions"></include>
    </select>
    
    <select id="getConsumeDataByDay" resultType="Map">
		SELECT
		    dt AS timeCycle,
		    SUM(consumer_integral) AS pointSum,
		    SUM(consumer_user) AS userSum
		FROM
		    cxb_integral_vn
		<include refid="conditions"></include>
		GROUP BY
		    timeCycle
    </select>
    
    <select id="getConsumeDataByWeek" resultType="Map">
		SELECT
		    DATE_FORMAT(dt,'%Y-%u') AS timeCycle,
		    SUM(consumer_integral) AS pointSum,
		    SUM(consumer_user) AS userSum
		FROM
		    cxb_integral_vn
		<include refid="conditions"></include>
		GROUP BY
		    timeCycle
    </select>
    
    <select id="getConsumeDataByMonth" resultType="Map">
		SELECT
		    DATE_FORMAT(dt,'%Y-%m') AS timeCycle,
		    SUM(consumer_integral) AS pointSum,
		    SUM(consumer_user) AS userSum
		FROM
		    cxb_integral_vn
		<include refid="conditions"></include>
		GROUP BY
		    timeCycle
    </select>
    
    <select id="getConsumeSummary" resultType="Map">
        SELECT
            SUM(consumer_integral) AS pointSum,
            SUM(consumer_user) AS userSum
        FROM
            cxb_integral_vn
        <include refid="conditions"></include>
    </select>
    

</mapper>